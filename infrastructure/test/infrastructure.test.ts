import * as cdk from 'aws-cdk-lib';
import * as Infrastructure from '../lib/constructs/car-viewer-frontend';

test('Stack Test', () => {
    const app = new cdk.App();
    // WHEN
    //Tests will be added later
});
